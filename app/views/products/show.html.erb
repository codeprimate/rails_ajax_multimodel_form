<p>
  <b>Name:</b>
  <%=h @product.name %>
</p>

<p>
	<ul>
		<% @product.assets.each do |asset|%>
			<li><%= render :partial => "products/asset", :locals => {:asset => asset} %></li>
		<% end%>
	</ul>
</p>


<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Back', products_path %>
<h1>New product</h1>

<% form_for(@product, :url => products_path, :html => { :method => :post, :multipart => true }) do |f| %>
   <%= f.error_messages %>
	<%= render :partial => "form", :locals => {:f => f}%>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', products_path %>